# 随机事件张力改写方案

> 诊断对象：`src/app/components/GamePage.tsx` 的 `EVENTS`（70 条）+ `src/app/eventBranches.ts` 的 `EVENT_BRANCHES`（67 事件 × 3 选项 = 201 条分支）+ `src/app/eventMeta.ts` 的权重系统
>
> 结论：**文笔没问题，结构有问题。** 现在的文案是一个很会写的人在写「保险的东西」。

---

## 一、量化诊断

我扫了全部 201 条分支文案，几个硬指标：

| 指标 | 数据 | 说明 |
|---|---|---|
| 使用破折号金句收尾 | 140 / 201 = **70%** | 「——原来XXX」成了固定收尾 |
| 最后一句含顿悟/总结/比喻词 | 60 / 201 = **30%** | 作者替玩家把感受消化完了 |
| tag 集中在四个词（务实/进取/社交/学习） | 65 / 201 = **32%** | 玩家第 10 个事件就学会元规则 |
| 代价落在他人身上的选项 | **1 / 201**（e02C 阴阳同门） | 全部选项都是「我付多少」 |
| 明确「什么都不做」的选项 | **约 5 条** | 且都被写成「更安全的退让」 |
| 效果值域 | 基本在 ±15 以内 | 没有任何不可逆后果 |
| 跨事件记忆 | **0** | 你做过的事从不回来找你 |

### 六个根因

**1. 三选项对称结构本身就是保守机。**
`硬刚 / 务实 / 回避`、`进取 / 务实 / 求助`、`社交 / 学习 / 利他` —— 这三个模板覆盖了大半事件。张力的前提是「我不知道该选哪个」，现在是「我知道该选哪个，只是数值不同」。

**2. 所有代价都是自负的。**
熬夜、掉好感、掉钱、涨自我怀疑 —— 这些只是**痛**，不是**张力**。真正的道德张力结构是：*你必须用别人换自己，或者用自己的一部分换自己的另一部分。*

**3. resultText 在替玩家顿悟。**
你把顿悟送给玩家，玩家就不需要自己顿悟了。「你突然明白」「原来……」「你第一次觉得」出现 19 次，这些句子每出现一次，读者的参与就被剥夺一次。

**4. 选择没有记忆。**
`ActionMemory` 追踪了行为次数（改图几次、投实习几次），但**没有追踪玩家做过的道德选择**。玩家 e02 阴阳了同门，后面 60 个事件里没有任何一个提到这件事。灵魂张力来自「你做过的事回来找你」，这个回路是断的。

**5. 没有一个选项是不可逆的。**
所有事件当场结算完毕、当场翻页。没有任何一步会让你「这一步走错，后面三回合全崩」。没有不可逆性 = 没有重量。

**6. tag 词本身在泄题。**
「务实」这两个字就在告诉玩家「这是安全选项」。tag 应该是**不带道德评价的动作名词**，不是评语。

---

## 二、六条改写律

### 律 1 — 删掉最后一句

70% 的文案在最后用破折号接一句总结。**那句话是作者在替玩家感受，删掉它。**
只留前面最具体、最冰冷、最不解释的物理细节。玩家自己想出来的那句话，永远比你写的更狠。

> ❌ 你保住了睡眠，但丢了质量——这个交易在建筑学院每天都在发生，你是今天轮到的那一个。
> ✅ 你保住了睡眠。导师回复：「框架可以，细节太糙。」

### 律 2 — 代价必须有第二个落点

每个事件至少留一个选项，让代价**落在一个有名字的人身上**。伤害必须具体：
不是「人脉 −5」，而是「李哲那个内推名额后来给了别人」。

### 律 3 — 给「什么都不做」一个按钮

现在 201 个选项全是动词。人生里最锋利的选择是**「我看到了，我关掉了手机」**。
关键：不作为**不能是更安全的选项**。它要有独立的、更慢、更钝的代价。

### 律 4 — 让选择长出记忆

需要一个 `choiceLedger`（道德账本）。玩家在 e02 阴阳过同门，第 4 学期那个同门成了面试他的人。
没有记忆的选择只是数值，有记忆的选择才是命运。

### 律 5 — 至少一个选项，玩家事后无法确认自己选对了

不要在结果文案里告诉玩家收益。**让他不知道。**

> ✅ 你不知道那句话有没有起作用。三周后你收到了面试通知，但你永远不会知道是因为哪一句。

### 律 6 — 留 3~5 个不可逆节点

整局游戏里安排 3~5 个「选了就锁死」的选项：永久锁死导师好感上限、永久移除某几个结局、永久关闭某条赛道。
不需要多，玩家撞上一次就会开始认真读文案。

---

## 三、样例改写（可直接粘贴）

### 样例 A：e01 导师催稿 —— 纯文本层，不动结构

只应用律 1 和律 5，结构完全不改。用来验证「同样的选项，写法能拉开多大差距」。

```ts
"e01": [
    {
        "id": "A",
        "label": "通宵肝完",
        "tag": "熬",
        "effects": { "arch": 6, "stress": -10, "selfDoubt": 3 },
        "resultText": "凌晨四点四十七分，你按下保存。发送。\n第二天早上七点，导师回了一个字符：。\n你把那个句号复制到搜索框里，想确认它是不是别的什么符号。它就是一个句号。\n对面宿舍楼那扇亮着 CAD 暖光的窗户，今晚没亮。"
    },
    {
        "id": "B",
        "label": "先睡两小时再改",
        "tag": "折",
        "effects": { "arch": 1, "stress": -5, "mentorFavorability": -5, "selfDoubt": 4 },
        "resultText": "闹钟设在五点半。你按掉了第一次，第二次爬起来了。\n八点五十九分发出去。导师回：「框架可以，细节太糙。」\n你回了一个「好的老师，我再改」。\n然后你打开文件，看了二十分钟，什么都没有改。"
    },
    {
        "id": "C",
        "label": "发微信请求延期",
        "tag": "求",
        "effects": { "stress": 2, "selfDoubt": 6, "mentorFavorability": -6, "arch": -2 },
        "resultText": "你打了三遍才发出去：「老师，能不能宽限到周三？」\n已读。没有回复。\n周三你交了。周四组会上他说：「有些同学的时间管理需要加强。」\n说完他看了一眼投影，继续讲下一页。整场会没有人看你。\n散会后同门问你：「他刚才说谁啊？」\n你说：「不知道。」"
    }
]
```

**改了什么：**
- A：删掉「你不知道这是收到了还是就这样」的自我解释，换成一个荒谬的具体动作（把句号复制到搜索框）。结尾「今晚没亮」暗示那个人可能已经退了，但不解释。
- B：删掉「这个交易在建筑学院每天都在发生」的总结，换成「看了二十分钟，什么都没有改」—— 用行为暴露状态。
- C：把「你知道说的是你，但他说得对」这段自我审判，换成**你对同门撒了谎**。这比自我谴责狠一百倍，因为玩家会自己发现「我居然撒谎了」。
- tag 从「硬刚/取巧/示弱」改成单字「熬/折/求」—— 不带评价，不泄题。

---

### 样例 B：e02 同门拿到字节实习 —— 引入代价转移 + 不作为

现有 C 选项（群里阴阳怪气那段「燕子」反串）写成了段子，玩家会笑，不会痛。而且伤害没有后果。
改成 **4 选项**（UI 是 `.map` 渲染的，选项数量本来就不固定，不用改组件）：

```ts
"e02": [
    {
        "id": "A",
        "label": "私聊请教面经",
        "tag": "问",
        "effects": { "network": 5, "logic": 4, "expression": 2, "selfDoubt": 4 },
        "resultText": "「恭喜，能请教一下面经吗？」\n她秒回了一个文档链接，附一句：「你肯定也行。」\n文档 47 页，比任何课程笔记都详细。你照着整理自己的项目，发现了三个盲区。\n整理到第二个盲区的时候，你划到文档最下面，看到一行小字：\n「更新于 2 月 14 日。」那时候她已经拿到 offer 三个月了，而你们班群里没有一个人知道。"
    },
    {
        "id": "B",
        "label": "把她翘课的事在群里说出来",
        "tag": "掀",
        "effects": { "network": -10, "selfDoubt": 12, "expression": -3, "ageAnxiety": 5 },
        "resultText": "你敲了一行：「她这学期设计课点名到过几次啊？」\n发出去之后你把手机倒扣在桌上，去洗了个脸。\n回来时群里已经有十九条新消息，没有一条是你发的。\n晚上辅导员单独找了她。三天后她的实习推迟入职。\n她没有找你对质。她把班群退了。\n下学期开学，你在教学楼楼梯上遇见她，她说：「你好。」然后就走了。"
    },
    {
        "id": "C",
        "label": "把 offer 截图转发到另一个群",
        "tag": "转",
        "effects": { "network": -6, "selfDoubt": 10, "ageAnxiety": 4 },
        "resultText": "你把截图转发到另一个群，没加任何文字。\n三分钟后有人说：「她不是天天不来上课吗？」\n你没有回复。二十分钟里又有三个人接着说。\n一周后她发了一条朋友圈：「有些人真的很闲。」你不确定是不是在说你。\n她没有屏蔽你。她只是再也没有在群里发过任何东西。"
    },
    {
        "id": "D",
        "label": "不点赞，不回复，不打开",
        "tag": "静",
        "effects": { "selfDoubt": 8, "ageAnxiety": 6, "stress": -2, "arch": 2 },
        "resultText": "你没有点赞。没有回复。你把那个群设成了免打扰。\n那天晚上你把图画完了，睡了六个小时，是这个月最长的一次。\n第二学期开学，你发现群名从「建筑X班」改成了「建筑X班（就业交流）」。\n改群名的人是她。你还在群里。你从来没有说过一句话。"
    }
]
```

**四个选项各自的代价落点：**

| 选项 | tag | 代价落在 | 手法 |
|---|---|---|---|
| A 私聊请教 | 问 | 我（自尊） | 律 5 —— 结尾那行「更新于 2 月 14 日」揭示你被信息差抛下了三个月，但不解释 |
| B 掀翻她 | 掀 | **她**（实习推迟） | 律 2 —— 伤害具体、有后果、无法撤回 |
| C 转发截图 | 转 | **她**（社交性死亡） | 最狠的一条：你没说任何坏话，你只是转发了一张图 |
| D 什么都不做 | 静 | 未来的我 | 律 3 —— 不作为的代价是「你被留在原地，而世界改了名字」 |

> C 选项最后那句「她没有屏蔽你。她只是再也没有在群里发过任何东西。」——
> 它没有指控玩家，它只是陈述了一个玩家造成的、无法撤回的事实。这就是灵魂张力。

---

### 样例 C：e13 导师不让实习 —— 不可逆 + 延迟引爆

原三选项（据理力争 / 阳奉阴违 / 暂缓实习）全部当场结算、全部可恢复。加一个**不可逆选项**：

```ts
{
    "id": "D",
    "label": "当着全组的面说「那我退组」",
    "tag": "断",
    "effects": { "mentorFavorability": -40, "stress": -15, "selfDoubt": 10, "arch": -5 },
    "resultText": "你听见自己说：「那我退组。」\n会议室里没有人抬头。导师停了两秒，把论文翻到下一页：「行，你去教务处填表。」\n散会后没有人跟你说话。你回工位收东西，发现你的位置本来就没有名牌。\n两天后你申请转到另一个课题组，新导师问：「你为什么从他那儿出来的？」\n你说了。他点点头：「我了解一下情况。」\n三周后教务处通知你，转组申请被驳回。原因写着「接收方无接收意愿」。",
    "irreversible": {
        "hardCapMentorFavorability": 5,
        "thesisScoreDelta": -20,
        "lockEndings": ["academic_path", "design_institute_core", "phd_track"]
    }
}
```

**延迟引爆示例**（改造现有 B 阳奉阴违）：

```ts
{
    "id": "B",
    "label": "阳奉阴违，实习改远程",
    "tag": "藏",
    "effects": { "expression": 3, "network": 4, "selfDoubt": 5, "mentorFavorability": -3 },
    "resultText": "你说：「好的老师，我以科研为主。」\n然后你把实习改成远程，每周三下午躲在图书馆七楼开会。\n这个学期你没有被发现。",
    "pendingConsequence": {
        "afterTurns": 8,
        "eventId": "e13_payback"
    }
}
```

8 回合后自动触发 `e13_payback`：

```ts
{
    id: "e13_payback", title: "组会上多了一张截图",
    description: "组会最后一页 PPT 不是论文进度，是一张图书馆门禁记录的截图。周三下午，连续 11 周。\n导师说：「这个学期大家都很忙。」然后关掉投影。\n没有人问那是谁的记录。散会后他叫住你：「你留一下。」\n他没有提实习。他只说：「你下学期的开题，我建议你换个方向。」",
    effects: { mentorFavorability: -20, thesisScore: -15, selfDoubt: 12, stress: -10 },
    type: "negative",
}
```

---

### 样例 D：无选项事件 —— 剥夺能动性

**技术上零成本**：`acknowledgeEvent` 的逻辑是 `EVENT_BRANCHES[id] ?? []`，长度为 0 时走原结算路径。
所以**只要不给这个事件写 branches，它自动变成单按钮事件**。

```ts
{
    id: "e71", title: "组会上导师念了另一个人的名字",
    description: "组会最后，导师宣布国家级课题的参与名单。\n他念了四个名字。你的名字不在里面。\n念完他补了一句：「还有小 X，你也一起吧。」小 X 是去年入学的。\n散会后你去问是不是漏了你。他说：「哦，你不是要转行吗？」\n你说：「我没说过。」\n他说：「那可能是我记错了。」\n下周名单公示。你的名字还是不在里面。",
    effects: { mentorFavorability: -10, selfDoubt: 14, ageAnxiety: 8, stress: -8 },
    condition: ({ semester }) => semester >= 3,
    type: "negative",
}
```

按钮只有一个：`[ 知道了 ]`

> **没有选项，才是最有张力的时刻。你什么都做不了。**
> 这在叙事设计里叫「剥夺能动性」（agency denial）。全局用 2~3 次就够，但玩家会记很久。

---

## 四、系统层三个扩展

### 扩展 1：`choiceLedger` 道德账本（律 4 的地基）

在 `eventMeta.ts` 的 `ActionMemory` 里加一层：

```ts
export type ChoiceTrait =
  | "harmed_peer"      // 伤害过同门
  | "betrayed_belief"  // 违背过自己的原则
  | "stayed_silent"    // 关键时刻沉默
  | "took_shortcut"    // 走过捷径（抄图/降重/避税）
  | "gave_away"        // 把机会让给了别人
  | "burned_bridge";   // 撕破过脸

export interface ActionMemory {
  // ...existing fields
  choiceLedger: Partial<Record<ChoiceTrait, number>>;
  choiceLog: Array<{ eventId: string; optionId: string; turn: number }>;
}
```

在 `EventBranchOption` 上加可选字段：

```ts
export interface EventBranchOption {
  id: string;
  label: string;
  tag: string;
  effects: Partial<Record<EventBranchStatKey, number>>;
  resultText: string;
  traits?: ChoiceTrait[];                                  // 写入账本
  resultVariants?: Array<{                                 // 账本驱动的变体文案
    when: (ledger: Partial<Record<ChoiceTrait, number>>) => boolean;
    resultText: string;
  }>;
  irreversible?: { hardCapMentorFavorability?: number; thesisScoreDelta?: number; lockEndings?: string[] };
  pendingConsequence?: { afterTurns: number; eventId: string };
}
```

然后就能写这种东西：

```ts
resultVariants: [{
    when: (l) => (l.harmed_peer ?? 0) > 0,
    resultText: "面试官推门进来，你抬头，是她。\n她翻了一下你的简历，说：「我们开始吧。」全程她没有提第二学期那件事。\n她问了七个问题，每一个都很专业。\n结束时她说：「结果三天内会通知你。」然后她站起来，先出去了。"
}]
```

### 扩展 2：`pendingConsequences` 延迟引爆队列

```ts
interface PendingConsequence { eventId: string; triggerTurn: number; sourceOptionId: string; }

// GamePage 里加 state
const [pendingConsequences, setPendingConsequences] = useState<PendingConsequence[]>([]);

// 在 maybeShowEvent 最前面插一段：到期的强制引爆，优先级高于 getCausalEvent
const due = pendingConsequences.filter(p => p.triggerTurn <= currentTurn);
if (due.length > 0) {
  const forced = EVENTS.find(e => e.id === due[0].eventId);
  if (forced) { setCurrentEvent(forced); setPendingConsequences(prev => prev.filter(p => p !== due[0])); return; }
}
```

### 扩展 3：把 `EventBranchStatKey` 扩到全属性

现在分支只能改 11 个核心属性，`thesisScore` / `health` / `reputation` 都碰不到，
导致「代价」的表达空间被人为压缩了一半。改成：

```ts
export type EventBranchStatKey =
  | "arch" | "logic" | "expression" | "english" | "structured"
  | "stress" | "network" | "money" | "selfDoubt" | "ageAnxiety" | "mentorFavorability"
  | "thesisScore" | "health" | "reputation" | "execution" | "empathy"
  | "riskTolerance" | "infoChannels" | "commercial" | "negotiation";
```

有了 `thesisScore` 和 `health`，才能写出「你赢了这一局，但论文估分掉了 15 分」这种真正的两难。

---

## 五、落地优先级

| 优先级 | 动作 | 成本 | 收益 |
|---|---|---|---|
| **P0** | 按律 1 洗一遍全部 201 条 resultText，删掉总结句 | 纯文本，1 天 | 立刻见效，玩家会觉得「变冷了、变准了」 |
| **P0** | tag 全部换成单字/双字动作名词，去掉评价色彩 | 0.5 天 | 消除泄题 |
| **P1** | 挑 15 个高频事件，各加 1 个「代价转移」或「不作为」选项 | 2 天 | 张力质变的主战场 |
| **P1** | 扩展 `EventBranchStatKey` 到全属性 | 30 分钟 | 解锁两难的表达空间 |
| **P2** | 加 3 个无选项事件（零代码，只写 EVENTS 不写 branches） | 半天 | 单点冲击力最强 |
| **P2** | 实现 `pendingConsequences`，配 5 个延迟引爆 | 1 天 | 让玩家开始害怕自己的选择 |
| **P3** | 实现 `choiceLedger` + `resultVariants` | 2 天 | 终极形态：选择长出记忆 |
| **P3** | 标注 3~5 个 `irreversible` 节点 | 1 天 | 给整局游戏加上重量 |

---

## 附：一句话总结

现在的文案在写**「转行有多难」**。
要写的是**「为了转行，你变成了什么样的人」**。

前者读者会同情，后者读者会照镜子。
