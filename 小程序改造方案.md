# 「建筑转行模拟器」→ 微信小程序改造全方案

> 当前项目：React 18 + TypeScript + Vite 6 + Tailwind CSS 4 + Supabase  
> 目标平台：微信小程序  
> 推荐框架：**Taro 4.x + React**（最大化代码复用）

---

## 一、当前资产盘点

| 文件 | 行数 | 迁移难度 | 说明 |
|------|------|---------|------|
| `GamePage.tsx` | 5072 | ⚠️ 高 | 核心游戏逻辑 + UI，需拆分为多页面 |
| `eventBranches.ts` | 2362 | ✅ 低 | 纯数据，几乎直接复用 |
| `AIAssistant.tsx` | 522 | ⚠️ 中 | fetch + dangerouslySetInnerHTML 需改 |
| `StatusAnalysisPanel.tsx` | 108 | ⚠️ 中 | SVG 雷达图需用 Canvas 重写 |
| `DesktopGameSidebar.tsx` | 460 | ❌ 重做 | 桌面端专属，移动端重新设计 |
| `knowledgeBase.ts` | 272 | ⚠️ 中 | Supabase RPC + Embedding API 需适配 |
| `llm.ts` | 102 | ✅ 低 | fetch → Taro.request 即可 |
| `supabase.ts` | 19 | ❌ 需替代 | Supabase SDK 不支持小程序 |
| `gameUiFlags.ts` | 3 | ✅ 低 | 直接复用 |
| 图片资源 | 175个 | ❌ 需迁移 | 全部上传 CDN/云存储 |

---

## 二、Phase 1：搭建 Taro 脚手架

### 2.1 安装与初始化

```bash
# 全局安装 Taro CLI
npm install -g @tarojs/cli

# 创建新项目（选 React + TypeScript）
taro init architecture-career-miniapp

# 选项：
#  框架: React
#  语言: TypeScript
#  CSS: Less/Sass
#  模板: default
#  包管理: npm
```

### 2.2 安装必要依赖

```bash
cd architecture-career-miniapp

# Tailwind 小程序适配
npm install weapp-tailwindcss postcss-rem-to-responsive-pixel --save-dev

# Taro 插件：HTML 渲染
npm install @tarojs/plugin-html

# Supabase 替代：使用 REST 客户端
npm install axios

# 状态管理（建议引入，替代 useState 散落）
npm install zustand
```

### 2.3 目录结构规划

```
architecture-career-miniapp/
├── config/
│   ├── index.ts          # Taro 配置
│   └── dev.ts / prod.ts
├── src/
│   ├── app.tsx           # 入口（替代原 main.tsx）
│   ├── app.config.ts     # 全局路由配置
│   ├── app.less          # 全局样式
│   │
│   ├── pages/            # 页面拆分（替代原单页 GamePage）
│   │   ├── create/       # 角色创建页
│   │   ├── game/         # 主游戏页（回合制核心）
│   │   ├── status/       # 能力诊断中心
│   │   ├── assistant/    # AI 助手（建哥）
│   │   └── ending/       # 结局展示页
│   │
│   ├── store/            # Zustand 全局状态
│   │   └── gameStore.ts  # 游戏状态管理
│   │
│   ├── data/             # 纯数据层（从原项目迁移）
│   │   ├── eventBranches.ts    # 事件分支数据
│   │   ├── knowledgeBase.ts    # 本地知识库
│   │   ├── schools.ts          # 学校数据（从 GamePage 剥离）
│   │   └── constants.ts        # 常量定义
│   │
│   ├── services/         # API 服务层
│   │   ├── llm.ts              # AI 接口
│   │   ├── knowledge.ts        # 知识库搜索
│   │   └── analytics.ts        # 统计上报
│   │
│   ├── components/       # 公共组件
│   │   ├── StatBar/      # 属性条
│   │   ├── EventCard/    # 事件卡
│   │   ├── ActionButton/ # 行动按钮
│   │   └── RadarChart/   # 雷达图（Canvas）
│   │
│   └── utils/            # 工具函数
│       ├── storage.ts    # 封装 wx.storage
│       └── format.ts     # 格式化
```

### 2.4 Taro 配置关键项

```typescript
// config/index.ts
const config = {
  projectName: 'architecture-career-miniapp',
  date: '2026-08-02',
  designWidth: 375,        // 以 iPhone 6 为设计基准
  deviceRatio: {
    640: 2.34 / 2,
    750: 1,
    828: 1.81 / 2,
    375: 2 / 1,
  },
  plugins: [
    '@tarojs/plugin-html',   // 支持 HTML 标签
  ],
  // 分包配置（体积控制关键）
  subPackages: [
    {
      root: 'pages/assistant',
      pages: ['index'],
    },
    {
      root: 'pages/ending',
      pages: ['index'],
    },
  ],
}
```

---

## 三、Phase 2：迁移核心数据层

这一阶段**不改 UI，只搬逻辑**。目标是让数据定义和纯函数在新项目中直接跑通。

### 3.1 搬数据文件（几乎零改动）

```bash
# 直接复制这些文件到 src/data/
复制: src/app/eventBranches.ts → src/data/eventBranches.ts
复制: src/lib/knowledgeBase.ts → src/data/knowledgeBase.ts
复制: src/app/gameUiFlags.ts   → src/data/gameUiFlags.ts
```

### 3.2 从 GamePage.tsx 剥离数据

GamePage.tsx 的第 15-88 行（学校数据、常量、类型定义）直接搬到独立文件：

```typescript
// src/data/schools.ts
export const SCHOOLS_BY_TIER: Record<number, string[]> = { /* ... */ };
export const OVERSEAS_SCHOOLS = [ /* ... */ ];
export const TIER_LABELS: Record<number, string> = { /* ... */ };

// src/data/constants.ts
export const SEMESTER_LABELS: Record<number, string> = { /* ... */ };
export const STAT_META = { /* ... */ };
```

### 3.3 将游戏核心逻辑抽出为纯函数

GamePage.tsx 中有大量状态变更逻辑（属性计算、回合推进、结局判定等），这些是**平台无关的纯 TypeScript**，应该抽出来：

```typescript
// src/core/gameEngine.ts  ← 新建，平台无关

export interface GameState {
  stats: Stats;
  semester: number;
  round: number;
  character: CharacterInfo;
  // ...
}

// 纯粹的计算函数，不依赖任何框架
export function applyEffects(state: GameState, effects: Partial<Stats>): GameState { /* ... */ }
export function checkEnding(state: GameState): EndingType | null { /* ... */ }
export function advanceRound(state: GameState): GameState { /* ... */ }
export function rollRandomEvent(semester: number): EventBranchOption[] { /* ... */ }
```

> **这一步最重要** —— 做完后，你的游戏核心逻辑成了一堆纯函数，可以单元测试，也能在 Taro 中无痛调用。

### 3.4 引入 Zustand 替代 useState

原 GamePage 用了几十个 useState，在 Taro 多页面架构下难以共享状态：

```typescript
// src/store/gameStore.ts
import { create } from 'zustand';
import { GameState, applyEffects } from '../core/gameEngine';

interface GameStore extends GameState {
  // 通过 actions 方法统一修改
  performAction: (actionId: string) => void;
  handleEventChoice: (optionId: string) => void;
  loadSave: (saveId: string) => void;
  resetGame: () => void;
}

export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  stats: { /* ... */ },
  semester: 1,
  round: 1,
  // ... actions
  performAction: (actionId) => {
    const state = get();
    const newState = applyEffects(state, ACTIONS[actionId].effects);
    set(newState);
  },
}));
```

---

## 四、Phase 3：页面拆分与路由

### 4.1 页面规划（5072 行 → 5 个独立页面）

| 页面 | 路由 | 来源 | 说明 |
|------|------|------|------|
| 角色创建 | `pages/create/index` | GamePage 初始化部分 | 选学校/导师/名字 |
| 主游戏 | `pages/game/index` | GamePage 核心循环 | 回合制主界面 |
| 能力诊断 | `pages/status/index` | StatusAnalysisPanel | 雷达图+短板分析 |
| AI 助手 | `pages/assistant/index` | AIAssistant | 建哥问答 |
| 结局展示 | `pages/ending/index` | GamePage 结局逻辑 | 分享海报 |

### 4.2 路由配置

```typescript
// src/app.config.ts
export default defineAppConfig({
  pages: [
    'pages/create/index',    // 首页：角色创建
    'pages/game/index',      // 主游戏
    'pages/status/index',    // 能力诊断
    'pages/assistant/index', // AI 助手（放入分包）
    'pages/ending/index',    // 结局（放入分包）
  ],
  window: {
    backgroundTextStyle: 'dark',
    navigationBarBackgroundColor: '#070c17',
    navigationBarTitleText: '建筑转行模拟器',
    navigationBarTextStyle: 'white',
  },
})
```

### 4.3 主游戏页架构示例

```tsx
// pages/game/index.tsx
import { View, Text, Button } from '@tarojs/components';
import { useGameStore } from '../../store/gameStore';
import { StatBar } from '../../components/StatBar';
import { EventCard } from '../../components/EventCard';
import { ActionGrid } from './ActionGrid';

export default function GamePage() {
  const { stats, semester, round, phase, chooseAction, handleEvent } = useGameStore();

  return (
    <View className='game-page'>
      {/* 顶部：学期/回合信息 */}
      <View className='game-header'>
        <Text>{SEMESTER_LABELS[semester]}</Text>
        <Text>第 {round} 回合</Text>
      </View>

      {/* 属性面板：横向滚动属性条 */}
      <View className='stats-bar'>
        {(Object.keys(STAT_META) as StatKey[]).map(key => (
          <StatBar key={key} label={STAT_META[key].label} value={stats[key]} color={STAT_META[key].color} />
        ))}
      </View>

      {/* 主体：根据 phase 切换 */}
      {phase === 'action_choice' && <ActionGrid onChoose={chooseAction} />}
      {phase === 'event_view' && <EventCard onChoice={handleEvent} />}
      {phase === 'action_result' && <ActionResultView />}
    </View>
  );
}
```

---

## 五、Phase 4：UI 层适配

### 5.1 Tailwind → WXSS 替换方案

Taro + `weapp-tailwindcss` 插件可以让 Tailwind class 在小程序中继续使用，但有限制：

**能用的：** 颜色 `text-blue-500`、间距 `p-4 m-2`、flex 布局、圆角、字体大小  
**不能用的：** `hover:` `focus:` 等伪类、`group`、`backdrop-blur`、`aspect-ratio`（部分）

```bash
# weapp-tailwindcss 配置
npm install weapp-tailwindcss --save-dev
```

```js
// config/index.ts
const { UnifiedWebpackPluginV5 } = require('weapp-tailwindcss/webpack')

module.exports = {
  // ...
  webpackChain(chain) {
    chain.merge({
      plugin: {
        install: {
          plugin: UnifiedWebpackPluginV5,
          args: [{ appType: 'taro' }]
        }
      }
    })
  }
}
```

### 5.2 关键 UI 改造清单

| 原实现 | 问题 | 小程序方案 |
|--------|------|-----------|
| `dangerouslySetInnerHTML` (AIAssistant 渲染消息) | 小程序禁用 | `<RichText nodes={...} />` |
| `<img>` 外部图片 | 需配置域名白名单 | CDN 图片 + `image` 组件 |
| `fixed bottom-4 right-4` (建哥悬浮按钮) | 可用，但需改为小程序定位 | `<View style='position:fixed'>` |
| `aspect-[16/9]` | 小程序不支持 | 计算高度或用 `mode='widthFix'` |
| SVG 雷达图 (StatusAnalysisPanel) | 小程序不原生支持 SVG | Taro Canvas 2D API 重绘 |
| `window.addEventListener('resize')` | 无此 API | `Taro.getSystemInfoSync()` |
| 桌面端侧边栏 `hidden lg:flex` | 移动端不需要 | 完全删除，底部 TabBar 替代 |

### 5.3 雷达图用 Canvas 重写

```tsx
// components/RadarChart/index.tsx
import { Canvas } from '@tarojs/components';
import Taro from '@tarojs/taro';

export function RadarChart({ stats }) {
  const canvasId = 'radar-canvas';

  useEffect(() => {
    const ctx = Taro.createCanvasContext(canvasId);
    const centerX = 110, centerY = 110, radius = 80;

    // 画网格
    [25, 50, 75, 100].forEach(v => {
      ctx.beginPath();
      // 计算多边形顶点...
      ctx.closePath();
      ctx.stroke();
    });

    // 画数据多边形
    // ...
    ctx.draw();
  }, [stats]);

  return <Canvas canvasId={canvasId} style='width:220px;height:220px' />;
}
```

### 5.4 移动端交互重设计

桌面端的功能 → 移动端简化方案：

| 桌面端 | 移动端替代 |
|--------|----------|
| 左侧固定侧边栏 (DesktopGameSidebar) | 底部 TabBar（游戏/状态/AI/设置） |
| 地图选点行动 (DesktopMapPreview) | 列表式行动选择卡片 |
| 电脑消息中心 (DesktopComputerPreview) | 剧情弹窗或信息流卡片 |
| 拖动窗口大小 (AIAssistant resize) | 全屏半屏切换 |
| hover 提示 | 长按/点击展开 |

---

## 六、Phase 5：API + 存储适配

### 6.1 Supabase 替代方案

Supabase JS SDK 依赖 WebSocket（实时订阅）和 `cross-fetch`，在小程序中不可用。三条替代路线：

**路线 A（推荐）：微信云开发**
```typescript
// 用微信云开发替代 Supabase
// 云函数中调用 Supabase REST API
// 小程序端通过 wx.cloud.callFunction 调用
```

**路线 B：直接 HTTP REST**
```typescript
// supabase 提供 REST API，直接 fetch
const response = await Taro.request({
  url: 'https://xxx.supabase.co/rest/v1/knowledge_chunks',
  method: 'POST',
  header: {
    'apikey': SUPABASE_ANON_KEY,
    'Authorization': `Bearer ${SUPABASE_ANON_KEY}`,
    'Content-Type': 'application/json',
  },
  data: { query_embedding: [...], match_threshold: 0.3, match_count: 5 }
});
```

**路线 C：纯本地知识库**
- 当前项目已有 26 条本地知识作为 fallback
- 如果知识库不大，可以直接本地关键词搜索
- 知识文本放在小程序本地，不依赖服务器

### 6.2 LLM API 调用

```typescript
// services/llm.ts
import Taro from '@tarojs/taro';

export async function askAssistant(query: string, contexts: string[]): Promise<string> {
  const key = Taro.getStorageSync('qwen_api_key');

  const res = await Taro.request({
    url: 'https://api-inference.modelscope.cn/v1/chat/completions',
    method: 'POST',
    header: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${key}`,
    },
    data: {
      model: 'Qwen/Qwen3.5-35B-A3B',
      messages: [
        { role: 'system', content: SYSTEM_PROMPT },
        ...contexts.map(c => ({ role: 'system', content: c })),
        { role: 'user', content: query },
      ],
      temperature: 0.7,
      max_tokens: 2000,
    },
  });

  return res.data.choices[0].message.content;
}
```

### 6.3 localStorage → wx.storage

```typescript
// utils/storage.ts
import Taro from '@tarojs/taro';

export const storage = {
  get<T>(key: string): T | null {
    return Taro.getStorageSync(key) || null;
  },
  set(key: string, value: any): void {
    Taro.setStorageSync(key, value);
  },
  remove(key: string): void {
    Taro.removeStorageSync(key);
  },
  // 存档管理
  listSaves(): SaveSlot[] {
    const saves: SaveSlot[] = [];
    for (let i = 1; i <= 3; i++) {
      const save = this.get<SaveSlot>(`save_slot_${i}`);
      if (save) saves.push(save);
    }
    return saves;
  },
};

// 全局替换：
// localStorage.getItem('key')     → storage.get('key')
// localStorage.setItem('key', v)  → storage.set('key', v)
```

### 6.4 API 域名白名单

在小程序管理后台 → 开发 → 开发管理 → 服务器域名中配置：

| 类型 | 域名 |
|------|------|
| request 合法域名 | `api-inference.modelscope.cn` |
| request 合法域名 | `xxx.supabase.co`（如果用 REST） |
| downloadFile 合法域名 | 你的 CDN 域名（图片托管） |

---

## 七、Phase 6：资源迁移 + 发布

### 7.1 图片资源迁移

当前 `public/` 下有 175 张图片（校徽、背景、地图等），必须搬到 CDN：

```bash
# 方案 A：上传到微信云存储
# 通过云开发控制台 → 存储管理 → 上传文件

# 方案 B：上传到阿里云 OSS / 腾讯云 COS
# 获取 CDN 域名后，全局替换图片路径

# 方案 C：对高频小图，可转 base64 内联
# 仅限 < 10KB 的图片
```

### 7.2 小程序审核要点

| 审核项 | 当前项目风险 | 应对 |
|--------|------------|------|
| 类目选择 | 需选「游戏」或「工具」 | 建议选「工具 → 信息查询」避免版号要求 |
| 用户生成内容 | AI 对话含 UGC | 需接入内容安全 API（`security.msgSecCheck`） |
| 分享功能 | 结局分享海报 | 使用 `wx.showShareMenu` 合规分享 |
| 登录 | 无需登录，纯本地游戏 | 无需 `wx.login`，降低审核复杂度 |

### 7.3 发布上线步骤

```bash
# 1. 构建微信小程序代码
npm run build:weapp

# 2. 打开微信开发者工具
#    导入 dist/ 目录

# 3. 预览 → 真机调试 → 上传

# 4. 小程序管理后台 → 版本管理 → 提交审核

# 5. 审核通过 → 发布
```

---

## 八、工作量估算

| 阶段 | 工作内容 | 预估工时 |
|------|---------|---------|
| Phase 1 | 搭脚手架 + 配置 + 目录 | 0.5 天 |
| Phase 2 | 剥离数据 + 纯函数 + Zustand | 1.5 天 |
| Phase 3 | 页面拆分 + 路由 + 主游戏页 | 2 天 |
| Phase 4 | UI 适配 + 雷达图重写 + 移动端设计 | 2 天 |
| Phase 5 | API 适配 + 存储迁移 | 1 天 |
| Phase 6 | 图片迁移 + 审核 + 上线 | 1 天 |
| **合计** | | **约 8 天**（全职） |

---

## 九、快速开始命令

```bash
# 第一步：安装 CLI
npm install -g @tarojs/cli

# 第二步：创建项目
taro init architecture-career-miniapp
# 选：React + TypeScript + Less

# 第三步：进入并安装依赖
cd architecture-career-miniapp
npm install zustand weapp-tailwindcss --save

# 第四步：复制数据文件
cp ../近代建筑史研究App/src/app/eventBranches.ts src/data/
cp ../近代建筑史研究App/src/lib/knowledgeBase.ts src/data/

# 第五步：启动开发
npm run dev:weapp
```

---

## 十、一句话总结

> **核心策略：数据层直接搬，核心逻辑抽纯函数，UI 层重写手机端，API 层换 Taro.request，图片全部上 CDN。**  
> 最大的工作不是写代码，而是把 5072 行的 GamePage.tsx **从中抽离出 3000+ 行纯逻辑**（这些逻辑一行都不用改），剩下 2000 行 UI 用 Taro 组件重写。
