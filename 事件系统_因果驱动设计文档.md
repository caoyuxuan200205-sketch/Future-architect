# 事件系统重构：从「纯随机」到「因果驱动」

> 版本：v1.0 | 日期：2026-08-14
> 范围：52 条主事件 + 4 条校园宣讲事件 + 61 组分支选项
> 核心改动：替换 `getRandomEvent()` 中的 `pick(available)` 为「行为权重 × 状态门槛 × 主题轮抽」三层算法

---

## 一、设计目标

| 目标 | 现状问题 | 目标状态 |
|------|---------|---------|
| 行为有后果 | 投 50 份简历和 0 份，出现"拒信"事件概率相同 | 高频投简历 → 拒信事件概率显著上升 |
| 状态有映射 | selfDoubt=90 和 30 时，失眠事件概率相同 | selfDoubt 高时，心理崩溃事件必触发 |
| 节奏有起伏 | 连续 5 个负面事件没有缓冲 | 同主题事件限频，正负交替 |
| 叙事有呼应 | 导师好感=5 时仍可能出现导师私活 | 低好感 → 关系恶化事件，高好感 → 导师认可事件 |
| 路径有差异 | 所有玩家经历的事件池完全相同 | 不同行为风格触发不同事件子集 |

---

## 二、事件分类标签体系

把 52 条主事件按 **主题（theme）** 和 **情绪（mood）** 打标签，作为权重计算基础。

### 2.1 六大主题线

| 主题代号 | 主题名 | 包含事件 | 核心关联行为 |
|---------|--------|---------|-------------|
| `mentor` | 导师关系线 | e01催稿, e07凌晨改图, e08私活, e13不让实习, e21消失, e22开题被毙, e36关系恶化, e40方向变更, e53打回18版, e59离谱安排 | 改图、送礼 |
| `job` | 求职转行线 | e02同门上岸, e03面试质疑, e06拒信, e09卡985, e10海归抢岗, e11HC冻结, e12实习低薪, e16雅思5.5, e29裁员, e32GPA, e34HR嫌经验, e37HR卡学历, e39错过窗口, e41面试通知, e44海归加剧, e48群面踩坑 | 投实习、参加校招、学产品 |
| `psych` | 心理状态线 | e04AIGC查重, e05学长延毕, e14家里催, e19知乎热帖, e23身体红灯, e25毕业晚会, e28战友回归, e31失眠, e43室友轻描, e56保研瓜, e62演唱会 | 摆烂、改图（消耗压力） |
| `growth` | 成长正向线 | e17认识学长, e18产品沙龙, e20竞品分享, e24大厂指导, e27改版最高分, e33Hackathon, e35文章爆了, e38学姐复盘, e42转行分享, e45提前批, e46校友交流, e47Campus Day, e50内推, e51遇贵人, e52人脉兼职, e57论文录用, e61转行论文 | 学产品、做副业、投实习 |
| `arch` | 建筑专业线 | e15设计院朋友圈, e26事务所实习, e54设计院800, e55租房坑, e58Mentor转工资, e60抄往届图 | 改图、做副业 |
| `social` | 社交人脉线 | e30家里支持, e50内推, e51遇贵人, e52人脉兼职 | 做副业、参加校招、送礼 |

### 2.2 事件完整分类表

| ID | 标题 | 主题 | 情绪 | 当前 condition | 备注 |
|----|------|------|------|----------------|------|
| e01 | 导师催稿 | mentor | negative | 无 | |
| e02 | 同门拿到字节实习 | job | negative | 无 | |
| e03 | 面试官质疑转行 | job | negative | 无 | |
| e04 | 论文AIGC查重超标 | psych | negative | 无 | |
| e05 | 学长延毕 | psych | negative | 无 | |
| e06 | 第十八封拒信 | job | negative | 无 | |
| e07 | 凌晨三点改图 | mentor | negative | 无 | |
| e08 | 导师让做私活 | mentor | negative | 无 | |
| e09 | JD写着985优先 | job | negative | 无 | |
| e10 | 海归抢同一岗位 | job | negative | !isOverseas | |
| e11 | HC冻结 | job | negative | 无 | |
| e12 | 实习工资不够房租 | job | negative | 无 | |
| e13 | 导师不让去实习 | mentor | negative | 无 | |
| e14 | 家里催问出路 | psych | negative | 无 | |
| e15 | 设计院朋友圈 | arch | negative | 无 | |
| e16 | 雅思5.5 | job | negative | english<65 | |
| e17 | 认识转行学长 | growth | positive | 无 | |
| e18 | 线下产品沙龙 | growth | positive | 无 | |
| e19 | 知乎热帖 | psych | negative | 无 | |
| e20 | 竞品分析全组分享 | growth | positive | 无 | |
| e21 | 导师消失两周 | mentor | negative | 无 | |
| e22 | 开题被毙 | mentor | negative | 无 | |
| e23 | 身体亮红灯 | psych | negative | 无 | |
| e24 | 大厂学长指导 | growth | positive | 无 | |
| e25 | 毕业晚会 | psych | negative | semester>=5 | |
| e26 | 拿到事务所实习 | arch | positive | 无 | |
| e27 | 改版方案最高分 | growth | positive | 无 | |
| e28 | 战友回归建筑 | psych | negative | 无 | |
| e29 | 互联网裁员新闻 | job | negative | 无 | |
| e30 | 家里表示支持 | social | positive | 无 | |
| e31 | 失眠连续一周 | psych | negative | 无 | |
| e32 | GPA不达标 | job | negative | 无 | |
| e33 | Hackathon二等奖 | growth | positive | 无 | |
| e34 | HR嫌缺乏互联网经验 | job | negative | 无 | |
| e35 | 转行分析文章爆了 | growth | positive | 无 | |
| e36 | 与导师关系恶化 | mentor | negative | 无 | |
| e37 | HR环节被卡学历 | job | negative | 无 | |
| e38 | 外企学姐复盘 | growth | positive | english>=45 | |
| e39 | 错过暑期实习窗口 | job | negative | 无 | |
| e40 | 课题组方向变更 | mentor | negative | 无 | |
| e41 | 收到第一个面试通知 | job | positive | 无 | |
| e42 | 非CS转行分享会 | growth | positive | 无 | |
| e43 | 宿舍同学轻描淡写 | psych | negative | 无 | |
| e44 | 海归竞争加剧 | job | negative | isOverseas | |
| e45 | 大厂提前批宣讲会 | growth | positive | semester>=3 | |
| e46 | 校友企业交流日 | growth | positive | semester>=2 | |
| e47 | 顶级外企Campus Day | growth | positive | english>=65 | |
| e48 | 宣讲会群面踩坑 | job | negative | semester>=4 | |
| e50 | 校友内推 | social | positive | network>=60 | |
| e51 | 行业交流会遇贵人 | social | positive | network>=50 && expression>=55 | |
| e52 | 人脉带来的兼职 | social | positive | network>=4 | |
| e53 | 改图改到18版被打回 | mentor | negative | 无 | |
| e54 | 设计院实习月薪800 | arch | negative | 无 | |
| e55 | 租房被中介坑 | arch | negative | 无 | |
| e56 | 吃到学妹保研的瓜 | psych | negative | 无 | |
| e57 | 论文被《建筑学报》录用 | arch | positive | 无 | |
| e58 | 设计院Mentor让转回工资 | arch | negative | 无 | |
| e59 | 设计课老师离谱安排 | mentor | negative | 无 | |
| e60 | OR系统抄往届图 | arch | positive | 无 | |
| e61 | 转行论文被录用 | growth | positive | 无 | |
| e62 | 那艺娜演唱会 | psych | positive | 无 | |

---

## 三、行为追踪系统（actionMemory）

在游戏 state 中新增 `actionMemory` 对象，记录玩家行为模式。

### 3.1 数据结构

```typescript
interface ActionMemory {
  // 累计行为次数
  totalActions: Record<string, number>;        // { revise: 5, product: 3, ... }
  
  // 连续行为次数（每次执行不同行动时重置对应计数）
  streak: Record<string, number>;              // { revise: 3 } 表示连续3次改图
  
  // 学期内行为次数（每学期末重置）
  semesterActions: Record<string, number>;     // 本学期各类行动次数
  
  // 关键里程碑
  totalInternships: number;                    // 累计投实习次数
  totalRejections: number;                     // 触发过的拒信事件数
  totalBurnout: number;                        // 摆烂总次数
  totalGifts: number;                          // 送礼总次数
  totalSidejobs: number;                       // 副业总次数
  mentorBetrayalCount: number;                 // 导师好感降破20的次数
  
  // 上次触发的主题（用于主题限频）
  lastTheme: string | null;                    // "mentor" | "job" | ...
  lastNegativeCount: number;                   // 连续负面事件数
}
```

### 3.2 更新时机

| 时机 | 更新内容 |
|------|---------|
| 玩家选择行动后 | `totalActions[actionId]++`、`streak[actionId]++`（其他归零）、`semesterActions[actionId]++` |
| 事件结算后 | `lastTheme = event.theme`；负面事件则 `lastNegativeCount++`，正面则归零 |
| 学期末 | `semesterActions` 全部归零 |
| 导师好感跌破 20 | `mentorBetrayalCount++` |

---

## 四、事件触发权重模型

### 4.1 核心公式

```
最终权重 = 基础权重 × 行为修饰因子 × 状态门槛因子 × 主题节奏因子
```

### 4.2 基础权重（baseWeight）

所有事件默认 `baseWeight = 1.0`。个别事件可以手工微调：

| 事件 | baseWeight | 原因 |
|------|-----------|------|
| e41 收到第一个面试通知 | 1.5 | 正向事件适当提高，给玩家希望 |
| e30 家里表示支持 | 1.3 | 正向情绪事件，防止过度压抑 |
| e56 保研瓜 | 0.6 | 猎奇事件，偶尔出现即可 |
| e60 OR系统抄图 | 0.5 | 隐藏彩蛋事件 |
| e62 演唱会 | 0.7 | 彩蛋事件 |

### 4.3 行为修饰因子（behaviorModifier）

这是本次重构的核心。每个事件关联一组行为修饰规则。

| 事件 | 修饰规则 | 设计意图 |
|------|---------|---------|
| **e06 第十八封拒信** | `投实习`连续 ≥3 次 → ×3.0；≥5 次 → ×5.0 | 投得越多，拒信越多 |
| **e11 HC冻结** | `参加校招` ≥2 次 → ×2.5 | 校招旺季才有的惨剧 |
| **e12 实习低薪** | `投实习` ≥4 次 → ×2.0 | 投得多才有概率踩到低价岗 |
| **e09 卡985** | `投实习` + `参加校招` 合计 ≥5 次 → ×2.0 | 求职量够了才会撞到学历墙 |
| **e01 导师催稿** | `改图`连续 ≥2 次 → ×2.5 | 越改越催 |
| **e07 凌晨三点改图** | `改图`连续 ≥3 次 → ×3.0 | 连续高强度改图必然熬夜 |
| **e53 改图18版打回** | `改图`累计 ≥6 次 → ×2.0 | 改图多了会遇到返工 |
| **e08 导师让做私活** | `mentorFavorability` ≥55 时 → ×2.0 | 导师信任你才找你干活 |
| **e36 关系恶化** | `mentorFavorability` ≤25 → ×3.0 | 好感低自然恶化 |
| **e21 导师消失** | `mentorFavorability` ≤35 → ×2.0 | 好感低导师不理你 |
| **e13 导师不让实习** | `mentorFavorability` ≤40 且 `投实习`≥2 → ×2.5 | 想走但导师不放 |
| **e17 认识转行学长** | `学产品`累计 ≥3 次 → ×2.0 | 开始学产品才进圈子 |
| **e18 产品沙龙** | `学产品` ≥2 次 → ×1.5 | 同上 |
| **e24 大厂学长指导** | `network` ≥45 → ×2.0 | 有人脉才遇到贵人 |
| **e50 校友内推** | `network` ≥60 → ×2.5 | 人脉够厚才有内推 |
| **e51 行业交流会遇贵人** | `network` ≥50 且 `expression`≥55 → ×2.0 | 已有 condition，权重再叠加 |
| **e35 文章爆了** | `做副业` ≥3 次 → ×2.0 | 做内容做多了才有爆款 |
| **e52 人脉兼职** | `做副业` ≥2 次 → ×1.5 | 做副业多了有人找 |
| **e19 知乎热帖** | `摆烂`连续 ≥1 次 → ×2.0 | 摆烂时刷手机看到焦虑内容 |
| **e14 家里催问** | `摆烂` ≥2 次 → ×2.5 | 摆烂久了家里必催 |
| **e31 失眠** | `stress` ≤30 时 → ×3.0 | 压力见底必失眠 |
| **e23 身体亮红灯** | `stress` ≤20 → ×3.0 | 压力极低身体崩溃 |
| **e16 雅思5.5** | `准备雅思` ≥2 次 → ×2.0 | 考过雅思才出成绩 |
| **e38 外企学姐复盘** | `english` ≥45 → ×1.5 | 英语够好才能进外企圈 |
| **e57 论文录用** | `arch` ≥60 且 `改图`≥5 → ×2.0 | 建筑论文强才有发表 |
| **e61 转行论文** | `学产品` ≥5 且 `做副业`≥2 → ×1.5 | 转行深度够才有研究产出 |
| **e54 设计院800** | `投实习` ≥3 且 `arch`≥55 → ×1.5 | 投建筑实习才踩到坑 |
| **e55 租房被坑** | `money` ≤30 → ×2.0 | 穷才被坑 |
| **e29 裁员新闻** | `semester` ≥4 → ×1.5 | 后期才有的宏观焦虑 |
| **e25 毕业晚会** | `semester` ≥5 → 固定必触发一次 | 已有 condition |

### 4.4 状态门槛因子（stateGate）

有些事件应该只在特定状态区间高概率触发，而不是完全锁死。

| 事件 | 状态门槛 | 权重修饰 |
|------|---------|---------|
| e31 失眠 | stress ≤ 25 | ×3.0；stress ≤ 15 时必触发一次 |
| e23 身体红灯 | stress ≤ 20 | ×3.0 |
| e36 关系恶化 | mentorFavorability ≤ 25 | ×3.0 |
| e02 同门上岸 | selfDoubt ≥ 60 | ×2.0（别人上岸时你更焦虑） |
| e19 知乎热帖 | selfDoubt ≥ 65 | ×2.5（自我怀疑高时才会搜这种帖子） |
| e28 战友回归 | selfDoubt ≥ 55 | ×2.0 |
| e43 室友轻描 | logic ≥ 40 | ×1.5（开始懂行了才对比） |
| e33 Hackathon | logic ≥ 45 && structured ≥ 40 | ×2.0（有能力才参加） |
| e35 文章爆了 | expression ≥ 50 | ×1.5（表达力够才写得出爆款） |

### 4.5 主题节奏因子（themeRhythm）

防止连续出现同主题或同情绪事件。

```typescript
function getThemeRhythm(event: GameEvent, memory: ActionMemory): number {
  let factor = 1.0;
  
  // 1. 连续 2 次同主题事件，第 3 次权重砍半
  if (memory.lastTheme === event.theme) {
    factor *= 0.3;
  }
  
  // 2. 连续 3 次负面事件，下一个负面权重 ×0.3，正面 ×2.0
  if (memory.lastNegativeCount >= 3) {
    if (event.type === "negative") factor *= 0.3;
    else if (event.type === "positive") factor *= 2.0;
  }
  
  // 3. 连续 2 次正面事件，下一个正面 ×0.5（防止太爽）
  if (memory.consecutivePositive >= 2 && event.type === "positive") {
    factor *= 0.5;
  }
  
  return factor;
}
```

---

## 五、新事件池分层设计

为了配合因果系统，建议把事件分为 **三个池**：

### 5.1 必触发事件（guaranteed pool）

某些关键叙事节点必须在特定条件下触发至少一次。

| 事件 | 必触发条件 | 原因 |
|------|-----------|------|
| e25 毕业晚会 | semester=6 第一回合 | 仪式感 |
| e41 第一封面试通知 | 投实习≥5次后必触发一次 | 给长期求职者希望 |
| e30 家里支持 | selfDoubt≥80 后必触发一次 | 绝境中的一束光 |
| e62 演唱会 | stress≤15 后必触发一次 | 精神救赎 |

### 5.2 因果触发事件（causal pool）

权重受行为和状态强烈影响的事件（即上面 4.3/4.4 列出的那些）。

### 5.3 常规随机事件（ambient pool）

权重基本不受行为修饰的事件，保持叙事丰富性。

---

## 六、新算法伪代码

```typescript
function getCausalEvent(
  seenIds: Set<string>,
  ctx: { stats: Stats; isOverseas: boolean; semester: number },
  memory: ActionMemory
): GameEvent | null {
  
  // 1. 过滤可见事件（保持原有 condition 检查）
  const available = EVENTS.filter(e => {
    if (seenIds.has(e.id)) return false;
    if (e.condition && !e.condition(ctx)) return false;
    return true;
  });
  
  if (available.length === 0) return null;
  
  // 2. 检查必触发池
  const guaranteed = checkGuaranteedPool(available, ctx, memory);
  if (guaranteed) return guaranteed;
  
  // 3. 计算每个事件的权重
  const weighted = available.map(event => {
    const base = EVENT_META[event.id]?.baseWeight ?? 1.0;
    const behaviorMod = getBehaviorModifier(event.id, memory, ctx.stats);
    const stateGate = getStateGate(event.id, ctx.stats);
    const rhythm = getThemeRhythm(event, memory);
    return { event, weight: base * behaviorMod * stateGate * rhythm };
  });
  
  // 4. 权重加权随机抽取
  const totalWeight = weighted.reduce((sum, w) => sum + w.weight, 0);
  let roll = Math.random() * totalWeight;
  for (const w of weighted) {
    roll -= w.weight;
    if (roll <= 0) return w.event;
  }
  return weighted[weighted.length - 1].event;
}
```

---

## 七、事件元数据配置表（EVENT_META）

新建 `EVENT_META` 常量，集中管理所有事件的标签和权重配置。

```typescript
export const EVENT_META: Record<string, {
  theme: "mentor" | "job" | "psych" | "growth" | "arch" | "social";
  mood: "positive" | "negative";
  baseWeight: number;
  behaviorModifier?: (m: ActionMemory, s: Stats) => number;
  stateGate?: (s: Stats) => number;
  guaranteed?: (m: ActionMemory, s: Stats, semester: number) => boolean;
}> = {
  "e01": {
    theme: "mentor", mood: "negative", baseWeight: 1.0,
    behaviorModifier: (m) => 1 + (m.streak.revice ?? 0) * 0.8,
    // 连续改图越多，催稿越频繁
  },
  "e06": {
    theme: "job", mood: "negative", baseWeight: 1.0,
    behaviorModifier: (m) => {
      const internStreak = m.streak.internship ?? 0;
      if (internStreak >= 5) return 5.0;
      if (internStreak >= 3) return 3.0;
      return 1.0;
    },
  },
  "e17": {
    theme: "growth", mood: "positive", baseWeight: 1.0,
    behaviorModifier: (m) => {
      const learnCount = m.totalActions.product ?? 0;
      if (learnCount >= 3) return 2.0;
      if (learnCount >= 1) return 1.5;
      return 0.8; // 没学过产品，不会认识转行学长
    },
  },
  "e31": {
    theme: "psych", mood: "negative", baseWeight: 1.0,
    stateGate: (s) => {
      if (s.stress <= 15) return 3.0;
      if (s.stress <= 25) return 2.0;
      if (s.stress <= 40) return 1.0;
      return 0.3; // 压力高（数值低=压力大）才失眠
    },
  },
  "e41": {
    theme: "job", mood: "positive", baseWeight: 1.5,
    guaranteed: (m) => (m.totalInternships >= 5 && !m.gotFirstInterview),
    // 投了5次以上必给一次面试通知
  },
  // ... 其余事件同理
};
```

---

## 八、事件因果链示例

设计一些**连锁反应**：A 事件的选择会影响 B 事件的触发。

### 8.1 导师线因果链

```
连续改图 ≥3 次
  → e01 导师催稿（权重 ×2.5）
    → 选 A "通宵肝完" → stress 大降
      → 下回合 e31 失眠（权重 ×3.0 if stress≤25）
    → 选 C "请求延期" → mentorFavorability -6
      → 累计好感 < 30 后
        → e36 关系恶化（权重 ×3.0）
          → 选 A "正面回应" → 关系继续恶化
            → e21 导师消失（权重 ×2.0）
          → 连续好感 < 20 触发结局 expelled
```

### 8.2 求职线因果链

```
学产品 ≥3 次
  → e17 认识转行学长（权重 ×2.0）
    → 选 A "系统学习资料" → logic +8
      → 后续 e18 产品沙龙（权重 ×1.5）
        → 选 A "主动 social" → network +12
          → network ≥ 60
            → e50 校友内推（权重 ×2.5）
              → 选内推 → 拿到面试机会
                → e41 第一封面试通知（guaranteed 触发）
```

### 8.3 心理崩溃线

```
连续负面事件 ≥3
  → 正面事件权重 ×2.0（系统自动给希望）
  
selfDoubt ≥ 80
  → e30 家里支持（guaranteed 触发一次）

stress ≤ 15
  → e62 演唱会（guaranteed 触发一次）
```

---

## 九、新增事件建议

因果系统需要一些**填补空白的连接性事件**：

| 建议 ID | 主题 | 触发条件 | 简介 |
|--------|------|---------|------|
| e63 | mentor | `送礼`≥3 次 且 mentorFav≥60 | 导师在组会上公开表扬你 |
| e64 | job | `投实习`≥8 次 且 0 offer | 招聘软件给你推"适合建筑生的互联网岗位" |
| e65 | growth | `学产品`≥5 且 `做副业`≥3 | 你的副业作品被一个初创团队看中，邀请合伙 |
| e66 | psych | `摆烂`≥3 且 `selfDoubt`≥75 | 深夜写的日记被你偶然翻到，看到三个月前的自己 |
| e67 | arch | `arch`≥70 且 `改图`≥8 | 你的课程作业被选入学院年展 |
| e68 | social | `network`≥70 且 semester≥5 | 你组织的转行经验分享会来了 100 人 |

---

## 十、实施步骤

| 阶段 | 任务 | 预计工时 |
|------|------|---------|
| 1 | 定义 `ActionMemory` 类型 + 初始化 + 更新逻辑 | 2h |
| 2 | 编写 `EVENT_META` 常量（52 条事件标签化） | 3h |
| 3 | 实现 `getBehaviorModifier()` / `getStateGate()` / `getThemeRhythm()` | 3h |
| 4 | 实现 `checkGuaranteedPool()` | 1h |
| 5 | 替换 `getRandomEvent()` 为 `getCausalEvent()` | 1h |
| 6 | 在主游戏循环中接入 `actionMemory` 更新点 | 2h |
| 7 | 编写 6 条新增事件 + 分支 | 2h |
| 8 | 测试 + 调参 | 3h |
| **合计** | | **~17h（2 天）** |

---

## 十一、风险与回退

| 风险 | 对策 |
|------|------|
| 权重参数过多，调参困难 | 先上线一套保守参数（行为修饰上限 ×3.0），观察数据再调 |
| 某些事件被行为修饰后永远触发不了 | 设置权重下限 0.1，保证所有符合 condition 的事件都有极小概率出现 |
| 玩家行为高度集中（如只改图），事件多样性下降 | 主题节奏因子强制限频，同主题连续 2 次后 ×0.3 |
| 新增事件文案量大 | 先实现系统框架，新事件分批迭代上线 |

---

## 附录：完整行为修饰映射表（精简版）

| 行为 | 提升权重的事件 | 降低权重的事件 |
|------|--------------|--------------|
| 改图 ↑ | e01催稿, e07熬夜, e53返工, e08私活, e57论文 | e17学长（微降） |
| 学产品 ↑ | e17学长, e18沙龙, e20分享, e38学姐, e61转行论文 | e01催稿（微降） |
| 投实习 ↑ | e06拒信, e09卡985, e11HC冻结, e12低薪, e41面试通知 | e08私活 |
| 参加校招 ↑ | e11HC冻结, e48群面, e29裁员 | — |
| 准备雅思 ↑ | e16雅思5.5, e38学姐, e47Campus Day | — |
| 做副业 ↑ | e35文章爆, e52人脉兼职, e65初创邀请 | e13导师不让（微降） |
| 送礼 ↑ | e63导师表扬（新） | — |
| 摆烂 ↑ | e14家里催, e19知乎帖, e66翻到日记（新） | e17学长, e18沙龙 |
